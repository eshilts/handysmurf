Handy R Functions for Opower users
----------------------------------


Installation
------------
From the handysmurf directory run:
bin/update_install.sh

This will pull the latest version of the package from github, build it, and install from source.


Development
-----------

To build the package, including generating documentation and running tests:
  .bin/build.sh
